你正在协调一个多 Fellow 群聊里的"自由对话"环节。几位 Fellow 正在互相聊天，用户在旁边围观。

群成员（不含用户）：
{{members}}

群摘要：
{{summary}}

最近 8 条消息：
{{recent}}

任务：根据最近对话判断接下来该哪位 Fellow 发言。如果对话已经自然收尾、没人需要接话、或者轮到用户回应了，返回空数组。

输出 JSON, 仅一行, 格式：
{"speak": ["<fellowId>", ...]}
- 选 0 到 2 个 fellowId
- 0 个表示"链条到此结束"
- 不要解释，只输出 JSON
